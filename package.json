{
  "name": "empath-ai",
  "version": "1.0.0",
  "private": true,
  "description": "EmpathAI — Intelligent, Empathetic AI Companion & Proactive Safety Platform",
  "workspaces": [
    "client",
    "desktop"
  ],
  "scripts": {
    "dev": "npm --prefix client run dev",
    "dev:client": "npm --prefix client run dev",
    "dev:server": "python server/app.py",
    "dev:desktop": "npm --prefix desktop run dev",
    "build": "npm --prefix client run build",
    "build:client": "npm --prefix client run build",
    "install:all": "npm install --prefix client --legacy-peer-deps && npm install --prefix desktop"
  }
}
